# --- Web framework ---
fastapi>=0.115,<1.0
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.9
pydantic-settings>=2.5
email-validator>=2.2          # required by pydantic's EmailStr

# --- Auth & crypto ---
pyjwt>=2.9                    # JWT issue/verify
bcrypt>=4.2                   # password hashing
cryptography>=43.0            # Fernet encryption for stored OAuth tokens

# --- Database ---
sqlalchemy[asyncio]>=2.0
asyncpg>=0.29                 # Postgres driver (production)
aiosqlite>=0.20               # SQLite driver (local dev, zero setup)
greenlet>=3.0                 # required by SQLAlchemy's async bridge
alembic>=1.13                 # migrations, once there is production data

# --- HTTP clients (VAPI, Google Calendar, WhatsApp Cloud API) ---
httpx>=0.27

# --- Dev / test ---
pytest>=8.3
pytest-asyncio>=0.24
